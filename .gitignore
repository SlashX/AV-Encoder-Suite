# Fisiere temporare ffmpeg
*.log
*.tmp
*_vidstab.trf
*_pass1.log
*_pass1.log.mbtree
ffmpeg2pass-*.log
ffmpeg2pass-*.log.mbtree

# Output video
*.mp4
*.mkv
*.mov
*.avi
*.dnxhd
*.dnxhr

# Output-uri ne-video (generate la runtime, NU se commit):
#  - telemetrie / GPS: CSV (_norm/_basic/_full), GPX, KML, SRT
#  - HDR/DV tools (v56): Plot PNG + Inspect RPU JSON export
# (niciun fisier de aceste tipuri nu e tracked; pattern-uri ca *.mp4)
*.csv
*.gpx
*.kml
*.srt
*_dvplot_*.png
*_rpu.json

# Profile salvate local (user profiles in UserProfiles/)
UserProfiles/
# Exclude src/profiles/ (built-in profiles, shipped with project)
!src/profiles/

# Temp folder (v36: Trim & Concat — creat lazy, NU se commit)
Temp/
src/Temp/

# Windows
Thumbs.db
desktop.ini

# Editor
*.swp
*.swo
*~
.vscode/
.idea/

# PowerShell
*.ps1xml

# Claude Code
CLAUDE.md

.claude/

# v43: test results + synthetic samples (regenerable)
tests/results/
tests/fixtures/samples/

# Python bytecode cache
__pycache__/
*.pyc

# v52: Binare locale (testing only — NU se commit, fiecare user le descarca local)
# Acoperite atat src/ cat si src/tools/ (depinde de unde le-a pus user / installer)
# ── ffmpeg suite (gyan.dev / BtbN, ~200MB combinate) ───────────────
src/ffmpeg.exe
src/ffprobe.exe
src/ffplay.exe
src/tools/ffmpeg.exe
src/tools/ffprobe.exe
src/tools/ffplay.exe
# ── Dolby Vision tools (quietvoid + sven-pke fork) ─────────────────
src/dovi_tool.exe
src/av1dovi_tool.exe
src/tools/dovi_tool.exe
src/tools/av1dovi_tool.exe
# ── HDR10+ tools (quietvoid + sven-pke fork) ───────────────────────
src/hdr10plus_tool.exe
src/av1hdr10plus_tool.exe
src/tools/hdr10plus_tool.exe
src/tools/av1hdr10plus_tool.exe
# ── ExifTool (binar + Perl runtime folder ~32MB) ───────────────────
src/exiftool.exe
src/exiftool_files/
src/tools/exiftool.exe
src/tools/exiftool_files/
# ── OpenAPV (validator de referinta APV — v69; build local / installer) ──
src/oapv_app_dec.exe
src/oapv_app_enc.exe
src/liboapv.dll
src/tools/oapv_app_dec.exe
src/tools/oapv_app_enc.exe
src/tools/liboapv.dll
src/tools/openapv_version.txt
# ── mkvmerge / MKVToolNix (v70 — dvcC de container pe hibride HEVC DV) ──
# Link static pe Windows → doar exe-ul (fara DLL-uri); installer in tools/
src/mkvmerge.exe
src/tools/mkvmerge.exe
src/mkvtoolnix_version.txt
src/tools/mkvtoolnix_version.txt
# ── MP4Box / GPAC (v71 — dvcC de container pe hibride HEVC DV pe MP4/MOV) ──
# MP4Box NU e standalone (are nevoie de libgpac.dll + av*.dll) → folder GPAC
# intreg sau install system-wide (winget); installer in tools/
src/mp4box.exe
src/tools/mp4box.exe
src/gpac/
src/tools/gpac/

# ── LUT-uri (.cube) — resurse de testare (testing only, NU se commit) ──
# Furnizate de utilizator (Apple/DJI/Samsung/creative, ~12MB fiecare,
# licentiate separat). Folderul src/Luts/ contine LUT-uri pentru testarea
# fluxului LOG → Rec.709 / HLG. Conventia de denumire e in docs/av_info.txt.
src/Luts/
*.cube